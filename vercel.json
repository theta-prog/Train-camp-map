{
  "buildCommand": "prisma generate && npm run build",
  "functions": {
    "src/app/api/**/*": {
      "maxDuration": 30
    }
  },
  "rewrites": [
    {
      "source": "/admin/(.*)",
      "destination": "/admin/$1"
    }
  ]
}
